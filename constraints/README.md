# Constraints not redistributed

The project used the course-provided `riscv.sdc` timing-constraint file. It is intentionally omitted from the public repository.

The completed run reports a **10.00 ns clock period (100 MHz)** for `clk_i`.
