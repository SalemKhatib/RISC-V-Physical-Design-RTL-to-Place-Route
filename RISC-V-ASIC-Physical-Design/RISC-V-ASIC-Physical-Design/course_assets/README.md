# Course helper scripts not redistributed

Place authorized local copies of the following course-provided helper scripts here only when reproducing the flow in the original course environment:

- `create_pg_network.tcl`
- `insert_boundary_and_tap_cells.tcl`
- `route.tcl`
- `filler.tcl`

These files are ignored by Git so they are not accidentally published.
